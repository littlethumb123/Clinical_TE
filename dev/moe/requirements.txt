# Runtime dependencies for:
# - dev/moe/exp_round10_training_inference_handoff.ipynb
# - dev/moe/moe_flashattn_4_core.py

# Install a PyTorch build that matches your CUDA runtime.
# See: https://pytorch.org/get-started/locally/
torch

numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
tqdm>=4.66

# BigQuery + pandas dataframe conversion used by the notebook.
google-cloud-bigquery>=3.25
db-dtypes>=1.2
pyarrow>=15.0

# Notebook execution support.
ipykernel>=6.29

# Downstream evaluation models imported in moe_flashattn_4_core.py.
xgboost>=2.0
lightgbm>=4.0

# Optional accelerators / performance extras.
# xformers is used if available; the code falls back when it is absent.
# Install a build compatible with your PyTorch and CUDA versions.
# xformers

# Optional BigQuery Storage API client for faster dataframe downloads.
# google-cloud-bigquery-storage