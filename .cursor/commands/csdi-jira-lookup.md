You are answering a question about Jira. Follow the jira-context-lookup skill workflow. Use the jira-conventions rule for project configuration. This flow is read-only.

The developer's question: {user's input after the command}

Execute the full context lookup workflow: interpret the question to JQL, execute the query, format results in a clean readable summary (never raw JSON), and report to the user. No confirmation needed — this is read-only.
