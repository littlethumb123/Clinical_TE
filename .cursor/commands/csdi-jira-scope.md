You are updating Jira scope mid-flight. Follow the jira-scope-change skill workflow. Use the jira-conventions rule for project configuration and jira-language rule for content translation. Confirmation is hook-enforced; still present full proposed changes before any write call.

The developer's scope change description: {user's input after the command}

Execute the full scope change workflow: identify affected issues, assess the change, draft all changes, present EVERYTHING for confirmation, execute only after approval. NEVER execute silently — full Confirmation Gate on every stakeholder-visible change.
